import React, { Component } from 'react';

class Settings extends Component {
    state = {  };
    render() {
        return (
            <div>Settings</div>
        );
    }
}

export default Settings;